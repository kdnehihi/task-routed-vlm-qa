# Data Directory

This directory is reserved for local datasets and processed artifacts.

- `raw/`: original downloaded or manually placed datasets.
- `processed/`: normalized task records, splits, cached metadata, and manifests.

TODO:

- Define a common JSONL schema for multi-task vision-language QA.
- Add dataset cards for DocVQA, ChartQA, TextVQA, VQA v2 subsets, and future scientific QA data.
- Add checks to prevent committing large datasets.

